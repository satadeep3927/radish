[package]
name = "radish-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
rustyline = "14.0"
bytes = "1.0"
colored = "2.1.0"
radish-proto = { path = "../radish-proto" }

[dev-dependencies]
radish-server = { path = "../radish-server" }

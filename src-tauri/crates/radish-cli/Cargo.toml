[package]
name = "radish-cli"
version = "0.2.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
rustyline = "14.0"
bytes = "1.0"
colored = "2.1.0"
radish-proto = { path = "../radish-proto" }

[dev-dependencies]
radish-server = { path = "../radish-server" }
